import React from 'react'
import Pane from './Pane'

const FilterCardsPane = ({board, onClose, gotoPane, goBack}) =>
  <Pane name="FilterCards">
    <div>Filter Cards Panel</div>
  </Pane>

export default FilterCardsPane
