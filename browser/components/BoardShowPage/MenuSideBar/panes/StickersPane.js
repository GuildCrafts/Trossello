import React from 'react'
import Pane from './Pane'

const StickersPane = ({board, onClose, gotoPane, goBack}) =>
  <Pane name="Stickers">
    <div>Stickers Panel</div>
  </Pane>

export default StickersPane
